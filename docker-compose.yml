version: "3.9"

services:
  qr-code-generator:
    build: .
    container_name: qr-code-generator
    ports:
      - "49155:3000"   # Map host port to container port
    volumes:
      - qr-output:/app/output
    restart: unless-stopped

volumes:
  qr-output:
